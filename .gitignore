node_modules/
dist/
build/
.env
.env.local

# server runtime data (uploaded PDFs, sqlite db) - never commit user documents
server/uploads/
server/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# demo PDFs are generated by the seed script, not checked in
server/demo-data/*.pdf

# client
client/dist/
client/.vite/

# Vercel local project link/build output - never committed
.vercel

# misc
.DS_Store
*.log
coverage/
